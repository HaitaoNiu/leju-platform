# 乐聚具身智能训练厂大中台管理平台

全栈管理平台项目，包含后端（FastAPI）和前端（React + Vite）。

## 项目结构

- `backend/` - 后端服务（FastAPI）
- `frontend/` - 前端应用（React + Vite）

## 快速开始

### 后端

```bash
cd backend
pip install -r requirements.txt
uvicorn main:app --reload
```

### 前端

```bash
cd frontend
npm install
npm run dev
```

